# Next.js 15 + SQLite SaaS — CLAUDE.md template

Opinionated root `CLAUDE.md` for a greenfield SaaS on **Next.js 15 App Router** + **SQLite** (better-sqlite3 locally / Turso in prod) with Drizzle, Zod, and Server Actions.

## Setup (3 steps)

1. Copy `CLAUDE.md` to the root of a new Next.js 15 + TypeScript project.
2. Keep stack choices as-is (pnpm, Drizzle, Zod, session auth, Stripe) — the file is designed to be used without edits.
3. Start Claude Code in that repo; it should follow structure, migrations, and anti-patterns without clarifying questions.

## What's inside

- Stack & pinned versions (with reasons)
- Folder layout + naming conventions
- SQLite PRAGMAs + Drizzle migration rules
- Server Component / Server Action patterns
- Explicit anti-patterns table

Opire bounty **#2 ($75)**.
